logging:
  config: classpath:log4j2.xml
spring:
  jpa:
    show-sql: true
    open-in-view: false
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:testdb